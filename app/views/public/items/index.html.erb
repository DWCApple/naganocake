<h1>商品一覧</h1>
<div>
  <% @items.each do |item| %>
    <td><%= image_tag item.image, size: "200x200" %></td>
    <td><%= link_to item.name, item_path(item.id) %></td>
    <td>$<%= item.price %></td>
</div>
<%= paginate @items %>
  <% end %>
