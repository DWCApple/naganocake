<h1>商品一覧</h1>
<div>
  <% @items.each do |item| %>
  　<td>
  　   <%= link_to item_path(item.id) do %>
      <%= image_tag item.image, size: "100x100" %>
      <% end %>
    </td>
    <td><%= item.name %></td>
    <td><%= item.price %>円</td>
</div>
<%= paginate @items %>
  <% end %>
